<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>TimestampCopy.Core</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- Lets the tests reach the registry command builder and the clipboard readers without
         going through the real HKEY_CLASSES_ROOT or the real %LOCALAPPDATA%. -->
    <InternalsVisibleTo Include="TimestampCopy.Tests" />
  </ItemGroup>

</Project>
